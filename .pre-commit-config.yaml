repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks secret scan
        entry: gitleaks
        args:
          - protect
          - --staged
          - --verbose
        language: system
        pass_filenames: false