repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks staged scan
        entry: bash
        args:
          - -c
          - |
            gitleaks protect --staged --verbose
        language: system
        pass_filenames: false